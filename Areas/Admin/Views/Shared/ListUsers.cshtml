
@model IEnumerable< Prototype.Areas.Admin.Models.Participant>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}


<h2>Participants Phase 1</h2>

<p>

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Participant Email
            </th>

            <th>
                Participant Data
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Participant_Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Participant_Data)
                </td>
                <td>
                    <a asp-page="/Edit">Edit</a> |
                    <a asp-page="/Details">Details</a> |
                    <a asp-page="/Delete">Delete</a>
                </td>
            </tr>
        }
        </tbody>
</table>
